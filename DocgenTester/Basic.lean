import Mathlib.Tactic.Basic

library_note2 «linking to library notes»
/-- You can now link to library notes! -/

library_note2 «just like this»
/-- It works just like this! -/

/-- See note [linking to library notes].

In fact, See library Note [just like this]. -/
def hello := "world"
